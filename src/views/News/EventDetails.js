import React from "react";
import styled from "styled-components";

const EventDetails = ({ data }) => {
  console.log();
  return <Wrapper>swag</Wrapper>;
};

export default EventDetails;

const Wrapper = styled.article`
  box-sizing: border-box;
  position: absolute;
  top: 0;
  left: 0;
  height: 100%;
  width: 100%;
`;
